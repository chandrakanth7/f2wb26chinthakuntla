extends layout

block content
    h1= title
    - var images  = ["image1", "image2", "image3", "image4", "image5"]
    - for(let i = 0; i < 3; i++) {
        - let number = Math.floor(Math.random() * 5)
        img(src=`/images/${images[number]}.jpg`, alt="", height="300px", width="300px")
    - }